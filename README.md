Text-to-SQL Query Generator
A Streamlit application that converts natural language questions into SQL queries and retrieves data from a SQLite database using OpenAI's GPT model.

🚀 Features
Natural Language to SQL: Convert English questions into valid SQL queries

SQLite Integration: Connect to and query a SQLite database

AI-Powered: Uses OpenAI's GPT-3.5-turbo model for intelligent query generation

User-Friendly Interface: Clean Streamlit web interface

Real-time Results: Display generated SQL queries and retrieved data

Error Handling: Graceful error handling with user-friendly messages

📋 Prerequisites
Before running this project, make sure you have:

Python 3.7+

OpenAI API key

SQLite database named student.db
